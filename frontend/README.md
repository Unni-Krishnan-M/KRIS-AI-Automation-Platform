# Frontend — Next.js 15

Per `docs/project-spec.json`, the frontend is **Next.js 15 + TypeScript**.

Full initialization (`create-next-app`, Tailwind, shadcn/ui, Zustand, TanStack Query,
React Hook Form, Zod, Framer Motion) happens in its own milestone
(**M13 — Analytics Dashboard**), so the backend is built and stable first.

This folder is intentionally a placeholder until then.
